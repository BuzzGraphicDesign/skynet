<?php 
/**
 * Skynet helper functions and definitions
 *
 * @package Skynet
 */

// Exit if accessed directly.
//defined( 'ABSPATH' ) || exit;

/*
	function theme_options_get_bg() {

		$bg_colors_array = [];

		get_field('style_primary_bg_color', 'option') != '' ? $bg_colors_array[get_field('style_primary_bg_color', 'option')] = 'Primary BG color' : false;
		get_field('style_secondary_bg_color', 'option') != '' ? $bg_colors_array[get_field('style_secondary_bg_color', 'option')] = 'Secondary BG color' : false;
		
		if (count($bg_colors_array) > 0) {
			return $bg_colors_array;
			//echo 'gfdgd';exit;
		}
		
		// fallback
		//return false;

	}


*/